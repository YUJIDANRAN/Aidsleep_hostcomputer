# 单通道 EEG 配合加速度计进行伪迹识别的思路

## 1. 背景与目标

在可穿戴睡眠监测或便携式 EEG 系统中，如果只采集单通道 EEG，系统结构简单、成本低、佩戴方便，但也会带来一个明显问题：很难判断某一段异常 EEG 波形究竟来自真实脑电活动，还是来自头动、身体翻动、电极松动、线缆牵拉等运动伪迹。

加速度计不能直接替代 EOG、EMG 或多通道 EEG，但它可以作为一个非常实用的辅助传感器，用于判断当前时段是否发生明显运动，从而提高单通道 EEG 数据质量评估和伪迹排除的可靠性。

本文档整理一种较容易落地的方案：使用单通道 EEG 观察脑电节律，同时使用三轴加速度计检测体动和电极扰动，对 EEG 数据进行标记、剔除、降权或暂停闭环刺激。

## 2. 单通道 EEG 的作用

单通道 EEG 可以用于以下任务：

- 观察特定脑区的节律变化，例如 alpha、theta、delta 等频段变化。
- 进行简单的清醒、困倦、入睡趋势判断。
- 检测部分睡眠相关事件，例如慢波、纺锤波趋势，但准确性有限。
- 用于低成本闭环声刺激系统，例如基于前额通道检测慢波相位。
- 做频段功率分析，例如 delta、theta、alpha、beta、gamma 的相对功率变化。

但单通道 EEG 的主要不足是：

- 空间信息少，不能判断异常是否具有全脑一致性。
- 难以区分脑电与眼动、肌电、体动、电极噪声。
- 对 REM、N1、N2 等睡眠阶段的精细区分能力有限。
- 对电极接触不良、头带滑动等问题较敏感。

因此，加速度计的主要价值不是替代 EEG 分析，而是帮助系统判断：这一段 EEG 是否可信。

## 3. 加速度计的作用

三轴加速度计通常输出：

```text
Ax, Ay, Az
```

在可穿戴 EEG 系统中，它可以用于检测：

- 头部运动；
- 身体翻身；
- 佩戴设备滑动；
- 电极接触状态变化；
- 大幅度姿态变化；
- 睡眠期间体动事件。

对睡眠 EEG 来说，体动常常会导致：

- EEG 出现大幅低频漂移；
- EEG 出现尖峰或阶跃；
- 高频能量异常升高；
- 某些频段功率被运动伪迹污染；
- 闭环声刺激算法误判慢波或相位。

因此，加速度计可以作为伪迹检测的重要辅助证据。

## 4. 数据同步

系统需要同时采集 EEG 与加速度数据，并保证两者有可对齐的时间轴。

推荐形式：

```text
time, eeg, ax, ay, az
```

或者：

```text
sample_index, eeg, ax, ay, az
```

如果 EEG 和加速度计采样率不同，也可以分别采集，再根据时间戳对齐。

示例：

```text
EEG: 250 Hz 或 500 Hz
ACC: 25 Hz、50 Hz 或 100 Hz
```

加速度计不需要和 EEG 一样高的采样率。对于体动检测，`25-100 Hz` 通常已经足够。

## 5. 加速度特征提取

### 5.1 合加速度

首先可以将三轴加速度合成为一个总加速度：

```text
Acc_mag = sqrt(Ax^2 + Ay^2 + Az^2)
```

这个值包含重力加速度和运动加速度。

### 5.2 去除静态重力影响

如果设备静止不动，`Acc_mag` 仍然会接近重力加速度。因此可以用局部中位数或均值作为基线：

```text
Motion = abs(Acc_mag - median(Acc_mag))
```

也可以使用差分：

```text
Motion_diff = diff(Acc_mag)
```

### 5.3 窗口化运动强度

将信号分成短窗口，例如 `1 s` 或 `2 s`，计算每个窗口的运动强度：

```text
Motion_energy = std(Acc_mag)
```

或：

```text
Motion_energy = rms(diff(Acc_mag))
```

含义：

- `std(Acc_mag)`：反映该窗口内加速度波动程度；
- `rms(diff(Acc_mag))`：反映加速度变化速度，对突然动作更敏感。

## 6. EEG 自身伪迹特征

加速度计只能说明是否发生运动，但不能单独判断 EEG 是否一定被污染。因此还需要结合 EEG 本身特征。

常用 EEG 伪迹特征包括：

### 6.1 幅值异常

如果 EEG 幅值突然过大，可能是运动伪迹、电极松动或肌电污染。

示例阈值：

```text
abs(EEG) > 100 uV
```

或：

```text
abs(EEG) > 150 uV
```

具体阈值需要根据设备增益、单位和实际数据调整。

### 6.2 突变程度

计算 EEG 差分：

```text
EEG_diff = diff(EEG)
```

如果短时间内变化过快，可能存在电极扰动或运动伪迹。

### 6.3 高频能量异常

肌电或电极摩擦常会造成高频能量升高。可以计算 beta/gamma 频段功率，或计算高频段相对功率。

例如：

```text
High_freq_ratio = power(20-40 Hz) / power(0.5-40 Hz)
```

如果该比值突然升高，需要标记为可疑。

### 6.4 低频大漂移

运动或电极位移可能导致明显低频漂移。这类伪迹可能落在 delta 频段内，因此尤其容易干扰睡眠慢波分析。

可以观察：

```text
Delta_power = power(0.5-4 Hz)
```

如果 delta 突然极高，同时加速度也显示运动，那么更可能是运动伪迹，而不是真实慢波。

## 7. 联合判定逻辑

推荐不要只依赖单一阈值，而是将加速度特征和 EEG 特征组合成一个伪迹评分。

示例规则：

```text
如果加速度运动强：
    artifact_score += 1

如果 EEG 幅值过大：
    artifact_score += 1

如果 EEG 差分过大：
    artifact_score += 1

如果 EEG 高频能量异常：
    artifact_score += 1
```

根据分数判断：

```text
artifact_score >= 2:
    高置信伪迹

artifact_score == 1:
    可疑片段

artifact_score == 0:
    暂认为干净片段
```

这种方式比单纯用加速度阈值更稳，因为有些动作不一定明显污染 EEG，而有些 EEG 伪迹也可能不是由身体运动造成的。

## 8. 推荐处理策略

### 8.1 标记

最温和的方式是在数据中标记伪迹段。

例如：

```text
clean
suspicious
artifact
```

在波形图上可以用不同背景颜色显示：

- 白色：可信；
- 浅黄色：可疑；
- 浅红色：高置信伪迹。

### 8.2 剔除

在计算频段功率、PSD、睡眠分期或训练模型时，可以剔除高置信伪迹段。

例如：

```text
如果 30 s epoch 中超过 20% 的 2 s 小窗口被标为 artifact：
    整个 30 s epoch 标为 artifact
```

### 8.3 降权

对于可疑片段，不一定完全删除，可以降低其权重。

例如：

```text
clean: 权重 1.0
suspicious: 权重 0.5
artifact: 权重 0.0
```

这适合连续趋势分析。

### 8.4 暂停闭环刺激

如果系统用于闭环声刺激，例如检测慢波 up-phase 后播放声音，那么运动伪迹期间应该暂停刺激触发。

原因是：

- 运动伪迹可能被误判为慢波；
- 电极扰动会影响相位估计；
- 用户翻身或觉醒时播放刺激可能干扰睡眠。

推荐逻辑：

```text
如果当前窗口为 artifact:
    暂停声音刺激

如果连续若干秒恢复 clean:
    重新允许触发
```

## 9. 简化算法流程

下面是一个适合工程实现的基础流程：

```text
1. 同步采集 EEG、Ax、Ay、Az
2. 将数据按 1 s 或 2 s 划分窗口
3. 每个窗口计算：
   - EEG 最大幅值
   - EEG 差分 RMS
   - EEG 高频能量比例
   - 加速度运动强度
4. 根据阈值计算 artifact_score
5. 输出每个窗口的质量标签：
   - clean
   - suspicious
   - artifact
6. 在 EEG 显示、PSD 分析、睡眠分期或闭环刺激中使用该标签
```

## 10. Python 风格伪代码

```python
for window in windows:
    eeg_w = window.eeg
    ax_w = window.ax
    ay_w = window.ay
    az_w = window.az

    acc_mag = sqrt(ax_w**2 + ay_w**2 + az_w**2)
    acc_motion = std(acc_mag)

    eeg_amp = max(abs(eeg_w))
    eeg_diff = rms(diff(eeg_w))
    high_freq_ratio = bandpower(eeg_w, 20, 40) / bandpower(eeg_w, 0.5, 40)

    artifact_score = 0

    if acc_motion > acc_threshold:
        artifact_score += 1

    if eeg_amp > eeg_amp_threshold:
        artifact_score += 1

    if eeg_diff > eeg_diff_threshold:
        artifact_score += 1

    if high_freq_ratio > high_freq_threshold:
        artifact_score += 1

    if artifact_score >= 2:
        label = "artifact"
    elif artifact_score == 1:
        label = "suspicious"
    else:
        label = "clean"

    save_label(window.start_time, window.end_time, label)
```

## 11. 阈值设置建议

阈值不建议直接固定照搬，应根据设备和数据进行标定。

可以先采集几类测试数据：

1. 静息闭眼不动；
2. 静息睁眼不动；
3. 轻微点头或转头；
4. 明显翻身；
5. 故意碰触电极或头带；
6. 咬牙或皱眉；
7. 正常睡眠片段。

然后观察每类数据中的：

- 加速度 `std`；
- EEG 峰峰值；
- EEG 差分 RMS；
- 高频功率比例；
- delta 异常增大情况。

根据统计分布选阈值。例如：

```text
acc_threshold = 静止状态均值 + 3 * 静止状态标准差
```

EEG 幅值阈值可以从 `100-150 uV` 附近开始试，但应结合实际硬件单位调整。

## 12. 与睡眠分析的关系

在睡眠分析中，加速度计辅助单通道 EEG 的典型用途如下：

| 模块 | EEG 作用 | 加速度计作用 |
|---|---|---|
| 频段分析 | 计算 delta/theta/alpha/beta/gamma | 标记运动污染片段 |
| 入睡趋势 | 观察 alpha 减弱、theta 增强 | 判断是否有体动或翻身 |
| 慢波检测 | 检测 0.5-4 Hz 慢波 | 避免把运动低频漂移误判为慢波 |
| 纺锤波检测 | 检测 11-16 Hz 短时波包 | 排除动作导致的异常波形 |
| 睡眠分期 | 提供脑电节律特征 | 提供体动/静止状态特征 |
| 闭环声刺激 | 判断刺激时机 | 运动时暂停触发 |

## 13. 局限性

加速度计虽然有用，但也有明显限制：

- 它不能识别眼动伪迹；
- 它不能识别所有肌电伪迹；
- 它不能判断 REM 的快速眼动；
- 它不能替代 EOG 和 EMG；
- 它只能说明设备或身体是否运动，不能直接说明 EEG 是否一定被污染；
- 电极接触不良有时不伴随明显加速度变化。

因此，加速度计适合做运动伪迹辅助判断，而不是完整伪迹处理方案。

## 14. 推荐系统方案

如果系统目标是低成本可穿戴 EEG 原型，可以采用：

```text
单通道 EEG + 三轴加速度计
```

适合：

- 频段功率观察；
- EC/EO alpha 对比；
- 入睡趋势观察；
- 简化睡眠状态判断；
- 初步闭环声刺激实验；
- EEG 数据质量标记。

如果目标是更可靠的睡眠分期或接近 PSG 的分析，建议升级为：

```text
EEG + EOG + EMG + 加速度计
```

其中：

- EEG：判断脑电节律、慢波、纺锤波；
- EOG：判断眼动、眨眼、REM；
- EMG：判断肌肉张力、REM 肌张力下降、肌电污染；
- 加速度计：判断体动、翻身、佩戴扰动。

## 15. 总结

单通道 EEG 可以用于简化 EEG 研究和可穿戴睡眠辅助系统，但它对伪迹非常敏感。加入三轴加速度计后，可以显著增强系统对运动伪迹、翻身、电极扰动的识别能力。

较推荐的工程策略是：

```text
不要试图用加速度计直接“修复” EEG，
而是用它给 EEG 片段打质量标签。
```

也就是说：

```text
EEG 负责分析脑电，
加速度计负责判断这一段 EEG 是否可能被运动污染。
```

在实际系统中，可以先实现：

1. 加速度运动强度计算；
2. EEG 幅值和差分异常检测；
3. 联合伪迹评分；
4. clean/suspicious/artifact 标签；
5. 在上位机图像中显示可疑区间；
6. 在频段分析和闭环刺激中跳过高置信伪迹段。

这是一条比较稳妥、工程上容易实现、也符合可穿戴 EEG 系统设计逻辑的路线。

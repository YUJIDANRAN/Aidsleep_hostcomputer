可以。先把结论放前面：

当前工程里的“拒绝策略”不是在判断“这个人脑状态异常”，而是在判断**这段 EEG 数据质量是否可能不可靠**。  
它分两级：

```text
rejected / 红色坏段：
    明显坏，原则上不建议用于分析

suspicious / 黄色可疑段：
    有点不对劲，需要提醒，但不一定是坏数据
```

代码主要在 [power_cal.py](E:/qt_project/pyqt/Host_computer_6/Host_computer_6/power_cal.py:297) 的 `build_threshold_rejection()` 里。

**整体流程**

现在是按 **1 秒一段** 来检查 EEG：

```text
原始 EEG 数据
→ 每 1 秒切成一个小片段
→ 对每个 1 秒片段算几个指标
→ 如果超过阈值，就给这一整秒打标签
```

注意：目前只是打标签，**没有删除数据、没有拼接、没有平滑**。

**一、红色坏段：ADC 贴边饱和**

规则大概是：

```python
segment <= 50 或 segment >= 4045
→ rejected
```

含义是：采集芯片是 12 bit ADC，理论范围大约是 `0–4095`。如果信号经常贴近 `0` 或 `4095`，说明它已经撞到上下限了。

可以理解成：  
麦克风录音爆音时，波形顶到天花板，真实声音已经被削掉了。EEG 这里也是类似，贴边后真实波形已经不可信。

它主要检测：

```text
电极脱落
接触严重异常
放大器/ADC 饱和
信号顶到硬件上限或下限
```

这是比较严重的，所以标红。

**二、红色坏段：1 秒内峰峰值过大**

规则是：

```python
max(segment) - min(segment) > 1200
→ rejected
```

也就是这一秒里最高点和最低点差得太大。

正常 EEG 是比较小的波动。如果 1 秒内突然从很低跳到很高，幅度特别夸张，通常不像真实脑电，更像采集问题。

它主要检测：

```text
大幅运动伪迹
电极被碰到
导线晃动
接触突然变差
很大的尖峰/跳变
```

为什么用 `max - min`？  
因为原始 ADC 可能整体偏高或偏低，但只要这一秒内部波动不大，未必是坏的。峰峰值看的是“这一秒抖得有多厉害”。

**三、红色坏段：偏离本秒中位数太远**

规则是：

```python
median = 本秒数据的中位数
max(abs(segment - median)) > 800
→ rejected
```

这个比峰峰值更像是在抓“单个点或少数点突然飞出去”。

中位数可以理解为这一秒里的“中心位置”。如果某个点离这个中心太远，就说明这一秒里出现了特别突兀的尖峰。

它主要检测：

```text
瞬间尖刺
突然跳点
短暂接触异常
某个采样点异常偏大/偏小
```

它和峰峰值的区别是：

```text
峰峰值：
    看这一秒最高点和最低点差多少

偏离中位数：
    看有没有某个点离正常中心特别远
```

两者有重叠，但一个抓整体大幅波动，一个抓突兀离群点。

**四、黄色可疑段：原始波形峰峰值相对背景偏大**

这个不是固定 `1200` 的红色阈值，而是自适应阈值：

```text
阈值 = max(200, 全部1秒片段峰峰值的中位数 + 5 * MAD)
```

如果某一秒的峰峰值超过这个阈值，就标黄。

这里的意思是：  
不光看“绝对大不大”，还看“相对于这条记录自己的背景，它是不是特别大”。

比如有的人整体信号比较小，有的人整体信号比较大。固定阈值有时不公平，所以用这一整段数据自己的背景水平做参考。

MAD 可以先简单理解成“稳健版波动程度”。  
它不像标准差那样容易被几个极端尖峰带偏。

它主要检测：

```text
比平时更抖的一秒
轻度运动伪迹
轻微电极扰动
可能有问题但还没严重到红色的波动
```

所以只标黄。

**五、黄色可疑段：相邻采样点跳变过大**

规则也是自适应：

```text
先计算每 1 秒内最大的相邻点差值
阈值 = max(60, 全部片段该指标的中位数 + 5 * MAD)
超过 → suspicious
```

它看的不是这一秒整体幅度，而是：

```text
前一个点到后一个点，有没有突然跳很多
```

正常 EEG 即使在波动，也一般不会出现非常突兀的单点跳跃。相邻点突然大跳，更像采集瞬间异常。

它主要检测：

```text
短促尖峰
采样瞬间跳点
电极轻微弹动
数字采样异常
突然的机械扰动
```

这个也只标黄，因为短促跳变不一定足以整段废掉，但值得提醒。

**六、黄色可疑段：δ 波 RMS 异常偏大**

这是你前面提到的那段：

```python
EEG_SUSPICIOUS_DELTA_RMS_MAD_MULT = 6.0
EEG_SUSPICIOUS_DELTA_RMS_RATIO = 2.0
```

流程是：

```text
原始 EEG
→ 滤出 δ 波 0.5–4 Hz
→ 每 1 秒算 RMS
→ 如果某秒 δ RMS 明显高于背景
→ 标黄
```

RMS 可以理解为“这一秒的平均能量/平均强度”。

最终阈值是：

```text
delta阈值 = max(
    δ RMS 中位数 * 2.0,
    δ RMS 中位数 + 6.0 * MAD
)
```

为什么专门看 δ 波？

因为 δ 是很低频的波。真实睡眠里 δ 当然有意义，但在短测试、睁闭眼、运动环境里，特别大的低频慢摆动也经常来自：

```text
身体/头部慢动
电极接触慢慢漂
导线晃动
基线慢漂
运动造成的大低频波动
```

所以它只标黄，不直接标红。  
因为 δ 高也可能是真实生理现象，不能粗暴删掉。

**目前没有哪些策略**

当前没有这些：

```text
没有对 alpha 单独做幅值拒绝
没有对 beta/gamma 单独做幅值拒绝
没有把黄色段删除
没有把红色段删除后重新拼接
没有对坏段做平滑修补
没有按模型窗口跳过坏段重新算特征
```

现在的功率分析和波形图还是直接用完整 raw 数据算，只是把坏段/可疑段背景标出来。

**怎么理解红色和黄色**

可以这样记：

```text
红色 rejected：
    数据本身大概率坏了，像硬件饱和、大跳变、大尖峰

黄色 suspicious：
    数据可能受干扰，但也可能是真实脑电变化，所以先提醒，不直接判死刑
```

对于后续喂模型，我建议的原则是：

```text
红色段：
    不喂模型，或该窗口直接标为低质量/无效

黄色段：
    不一定删除，但要把 suspicious_ratio / 原因作为质量信息一起记录

正常段：
    可以用于脑状态分析
```

当前这套策略本质上是一个**数据质量门卫**：先判断“这段 EEG 靠不靠谱”，再谈“脑处于什么状态”。这两个问题要分开，否则模型很容易把采集伪迹误学成脑状态。